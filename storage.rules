rules_version = '2';

// Public study library — anyone can read and upload files.
// Mirrors the open Firestore rules. Lock down with Firebase Auth later
// if you need to restrict who can upload.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
